import React from 'react';

function RegisterThree({ children }) {
    return (
        <div className="auth-top-border container _mb">
            { children }
        </div>
    );
};

export default RegisterThree;