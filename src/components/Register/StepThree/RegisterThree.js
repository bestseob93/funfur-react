import React from 'react';

const RegisterThree = ({ children }) => {
    return (
        <div className="register-top-border container">
            { children }
        </div>
    );
};

export default RegisterThree;