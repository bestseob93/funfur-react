import React from 'react';

const RegisterFour = ({children}) => {
    return (
        <div className="auth-top-border container _mb">
            { children }
        </div>
    );
};

export default RegisterFour;