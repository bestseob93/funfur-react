import React from 'react';

function ProductUpload({children}) {
    return (
        <div className="product-form-container">
            {children}
        </div>
    );
}

export default ProductUpload;