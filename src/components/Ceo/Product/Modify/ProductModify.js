import React from 'react';

function ProductModify({children}) {
    return (
        <div className="product-form-container">
            {children}
        </div>
    );
}

export default ProductModify;